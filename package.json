{
  "name": "log-post-request-to-file",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/json-server --watch db.json --port 8000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.2"
  }
}
